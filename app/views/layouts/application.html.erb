<!DOCTYPE html>
<html>
  <head>
    <title>Snakebay</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1>Snake Bay</h1>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <nav>
      <% if user_signed_in? %>
      <% if @heading != "Snakes For Sale" %>
        <%= link_to "Back", listings_path, :method => "get", :class => "light round-box"%>
      <% end %>
        <%= link_to "Add New Listing", new_listing_path, :method => "get", :class => "light round-box"%>
        <%= link_to "Logout", destroy_user_session_path(@user_current), :method => "delete", :class => "light round-box"%>
      <% end %>
    </nav>

    <%= yield %>
  </body>
</html>
